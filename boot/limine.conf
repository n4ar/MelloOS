# MelloOS Limine Configuration (v8.x format)

# Timeout in seconds before auto-booting the default entry
timeout: 3

# Default entry to boot
default_entry: 0

# Boot entries
/MelloOS
    protocol: limine
    kernel_path: boot():/boot/kernel.elf
